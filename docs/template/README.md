# 模板初始化

这个目录保留模板初始化入口，避免历史链接和协作规则指向空目录。

当前仓库已经完成从模板到产品仓库的初始化。后续如果需要回看或重做模板复制流程，请优先参考：

- 根目录 `AGENTS.md` 中的模板初始化约定
- `README.md` 和 `docs/README.md` 中的当前事实源
- `docs/architecture/` 中的运行架构说明

如果是在维护可复用模板本身，请回到本地模板仓库处理，不要把模板专用清单继续扩展到当前产品仓库里。
